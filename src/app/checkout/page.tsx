import { CheckoutClient } from "./CheckoutClient";

export const metadata = {
  title: "Checkout — GreenCrate",
  description: "Complete your workplace food order.",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
